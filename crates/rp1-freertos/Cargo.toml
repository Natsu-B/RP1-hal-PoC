[package]
name = "rp1-freertos"
version = "0.1.0"
edition = "2024"
links = "rp1_freertos"
build = "build.rs"

[dependencies]
rp1-hal = { path = "../rp1-hal", optional = true }

[features]
spi0-irq = ["dep:rp1-hal"]
i2c1-irq = ["dep:rp1-hal"]
